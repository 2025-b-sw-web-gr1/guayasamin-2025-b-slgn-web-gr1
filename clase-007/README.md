📚 BookStream - Catálogo de Libros con Express y Handlebars📖 Descripción del ProyectoEste proyecto es una aplicación web simple construida con Node.js y Express que simula una plataforma de catálogo de libros al estilo streaming (como Netflix o Spotify, pero para libros).El objetivo principal de este proyecto (Taller de la Clase 007) fue explorar un motor de plantillas web diferente a EJS para gestionar la renderización dinámica de datos en el lado del servidor.Características ClaveRenderizado Dinámico: Los datos del catálogo (título, autor, portada, disponible) se definen en el archivo server.js y se inyectan en las plantillas HTML.Diseño de Cuadrícula: Utiliza CSS para presentar los libros en un formato de tarjeta visual, imitando una interfaz moderna de streaming.Gestión de Activos Estáticos: Configuración de Express para servir correctamente imágenes (/portadas) y estilos (/styles).🛠️ Tecnologías UtilizadasComponenteTecnologíaPropósitoBackend / ServidorNode.jsEntorno de ejecución de JavaScript.Framework WebExpress.jsManejo de rutas, servidor HTTP.Motor de RenderizadoHandlebars (HBS)Renderización de plantillas del lado del servidor.Gestión de ArchivospathMódulo nativo para construir rutas absolutas.💡 Elección del Motor de Renderizado: Handlebars (HBS)El proyecto utiliza Handlebars en lugar de EJS, que fue el motor base visto en clase.Ventajas de HandlebarsSeparación Clara (Logic-less): Handlebars es un motor de plantillas "sin lógica" (logic-less). Esto significa que prohíbe la ejecución de código JavaScript arbitrario en las plantillas. Esto fuerza al desarrollador a mantener la lógica de la aplicación (manipulación de datos, filtros) estrictamente en el archivo del servidor (server.js), dejando el archivo .hbs dedicado solo a la presentación.Sintaxis Concisa: Utiliza la sintaxis de bigotes ({{...}}) para variables y estructuras predefinidas ({{#each}}, {{#if}}), lo que resulta en un HTML más legible y menos propenso a errores de sintaxis JavaScript.Custom Helpers: Permite la creación de funciones auxiliares (helpers) reusables para tareas complejas de formato o manipulación que se ejecutan antes de renderizar la vista.Desventajas de HandlebarsMayor Configuración Inicial: Requiere una configuración más detallada con express-handlebars y definir rutas de layouts.Curva de Aprendizaje: Si bien la sintaxis es simple, la restricción de lógica obliga a aprender la estructura de sus helpers y de cómo manejar los datos antes de pasarlos a la plantilla, lo que puede ser una curva inicial más pronunciada que la flexibilidad total de EJS.🖼️ Aplicación del Renderizado y Assets EstáticosRenderizado con HandlebarsCuando un usuario accede a la ruta raíz (/), Express ejecuta la siguiente secuencia de renderizado:Ruta: app.get('/', ...) es activada en server.js.Datos: El array libros se pasa a la vista: res.render('index', { libros: libros });.Plantilla: Handlebars toma la plantilla views/index.hbs.Bucle: Ejecuta el bloque {{#each libros}} para iterar sobre la lista.Inyección: Dentro del bucle, sustituye {{titulo}}, {{autor}} y {{portada}} con los valores correspondientes de cada objeto libro.Condicional: Evalúa la estructura {{#if disponible}} para mostrar si el libro está disponible o se lanza "Próximamente".Output: El resultado es un único archivo HTML listo para enviar al navegador.Servir Archivos EstáticosEl servidor está configurado para manejar los archivos que no son plantillas HTML (como imágenes y CSS) a través de middleware de Express:Imágenes (/portadas): La línea app.use('/portadas', express.static(path.join(\_\_dirname, 'portadas'))); crea una ruta virtual. Cuando la plantilla usa <img src="/portadas/cien\_anos.jpg">, Express busca el archivo en la carpeta física portadas/.Estilos (/styles): app.use(express.static(path.join(\_\_dirname, 'styles'))); permite acceder a los archivos CSS directamente desde la raíz (ej: <link rel="stylesheet" href="/style.css">).▶️ Cómo Ejecutar el ProyectoSigue estos pasos en tu terminal:1. RequisitosAsegúrate de tener Node.js instalado en tu sistema.2. Instalación de DependenciasEjecuta este comando en la carpeta raíz del proyecto para instalar Express y Handlebars:Bashnpm install express express-handlebars

3\. Estructura de CarpetasConfirma que la estructura de archivos es correcta:/tu-proyecto

|-- server.js

|-- portadas/           <-- Tus imágenes JPG/PNG

|-- styles/             <-- Tus archivos CSS

|-- views/

|   |-- layouts/

|   |   |-- main.hbs

|   |-- index.hbs

4\. Iniciar el ServidorEjecuta el servidor de la aplicación:Bashnode server.js

5\. AccesoAbre tu navegador y navega a la siguiente dirección:http://localhost:3000

