<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>BookStream - Plataforma de libros</title>
  <style>
    :root{
      --vino: #5B0E2D;
      --vino-dark: #490822;
      --accent: #F481B1;
      --card-bg: rgba(255,255,255,0.04);
      --glass: rgba(255,255,255,0.06);
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: linear-gradient(180deg,var(--vino) 0%, var(--vino-dark) 100%);
      color:#fff;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }
    header{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:1rem;
      padding:1rem 2rem;
      background: linear-gradient(90deg, rgba(0,0,0,0.12), transparent);
      border-bottom:1px solid rgba(255,255,255,0.04);
      position:sticky;
      top:0;
      z-index:10;
    }
    .brand{display:flex;align-items:center;gap:.75rem}
    .logo{
      width:44px;height:44px;border-radius:6px;
      background:linear-gradient(135deg,#ff6fa3,#7b1f3a);
      display:flex;align-items:center;justify-content:center;font-weight:700;
      box-shadow:0 4px 18px rgba(0,0,0,0.4);
    }
    .brand h1{font-size:1.1rem;margin:0}
    .search{
      flex:1;max-width:560px;margin:0 1rem;
    }
    .search input{
      width:100%;padding:.6rem .9rem;border-radius:8px;border:1px solid rgba(255,255,255,0.06);
      background:var(--glass);color:#fff;outline:none;
    }
    nav a{color:rgba(255,255,255,0.9);text-decoration:none;margin-left:1rem;font-weight:600;font-size:.95rem}
    main{padding:1.5rem 2rem}
    .hero{
      display:flex;align-items:center;gap:1.5rem;padding:1.25rem;border-radius:10px;background:linear-gradient(90deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));
      border:1px solid rgba(255,255,255,0.03);
    }
    .hero .info h2{margin:0;font-size:1.4rem}
    .catalog{
      margin-top:1.25rem;
      display:grid;
      grid-template-columns:repeat(auto-fill,minmax(170px,1fr));
      gap:1rem;
    }
    .card{
      background:var(--card-bg);
      border-radius:8px;
      padding:.6rem;
      position:relative;
      overflow:hidden;
      transition:transform .18s ease, box-shadow .18s ease;
      text-align:center;
    }
    .card:hover{transform:translateY(-6px);box-shadow:0 8px 30px rgba(0,0,0,0.5)}
    .cover{width:100%;height:230px;object-fit:cover;border-radius:6px;display:block}
    .meta{padding:.5rem 0}
    .meta h3{margin:.2rem 0;font-size:1rem}
    .meta p{margin:0;font-size:.85rem;color:rgba(255,255,255,0.8)}
    .badge{
      position:absolute;top:8px;left:8px;padding:.25rem .5rem;border-radius:999px;font-size:.78rem;
      background:rgba(0,0,0,0.45);backdrop-filter:blur(4px);
    }
    .available{background:linear-gradient(90deg,#34d399,#10b981);color:#022c1a}
    .soon{background:rgba(255,255,255,0.06);color:#fff}
    .read-btn{
      position:absolute;left:50%;top:50%;transform:translate(-50%,-50%) scale(.96);
      display:flex;align-items:center;gap:.5rem;padding:.45rem .6rem;border-radius:999px;
      background:linear-gradient(90deg, rgba(255,255,255,0.06), rgba(0,0,0,0.25));
      color:#fff;border:1px solid rgba(255,255,255,0.06);opacity:0;transition:opacity .12s ease, transform .12s ease;
    }
    .card:hover .read-btn{opacity:1;transform:translate(-50%,-50%) scale(1)}
    .footer{margin-top:2rem;color:rgba(255,255,255,0.6);font-size:.9rem;text-align:center;padding-bottom:2rem}
    @media(max-width:600px){
      header{padding:0.6rem 0.8rem}
      .cover{height:200px}
    }
  </style>
</head>
<body>
  <header>
    <div class="brand">
      <div class="logo">BS</div>
      <div>
        <h1 style="margin:0;font-size:1rem">BookStream</h1>
        <div style="font-size:.75rem;color:rgba(255,255,255,0.75)">Streaming de libros — lee al instante</div>
      </div>
    </div>

    <div class="search">
      <input placeholder="Buscar título, autor o género..." aria-label="Buscar libros" />
    </div>

    <nav>
      <a href="/">Inicio</a>
      <a href="#">Mi Biblioteca</a>
      <a href="#">Tendencias</a>
    </nav>
  </header>

  <main>
    {{{body}}}
  </main>

  <div class="footer">BookStream · Plataforma de lectura by Sara Guayasamin</div>
</body>
</html>